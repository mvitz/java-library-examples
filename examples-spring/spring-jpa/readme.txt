To run this example you need to pass following parameter to the JVM:

    -javaagent:$M2_HOME/repository/org/springframework/spring-instrument/3.0.0.RELEASE/spring-instrument-3.0.0.RELEASE.jar

$M2_HOME must point to your maven2 installation